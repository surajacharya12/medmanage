import 'package:flutter/material.dart';

class MedicineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Medicine")),
      body: Center(child: Text("Welcome to the Medicine Screen")),
    );
  }
}
