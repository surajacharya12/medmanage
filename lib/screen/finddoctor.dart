import 'package:flutter/material.dart';

class Finddcoctor extends StatelessWidget {
  const Finddcoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
